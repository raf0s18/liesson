OPENAI_API_KEY=sk-your-key
# A random 32+ character string used to encrypt the anonymous lesson cookie.
LESSON_COOKIE_SECRET=replace-with-a-long-random-secret
# Vercel Dashboard → Storage → Create Database → KV, then attach it to this project.
KV_REST_API_URL=
KV_REST_API_TOKEN=
