import { test, expect } from "@playwright/test";
test("anonymous learner can complete a lesson",async({page})=>{await page.route("**/api/lesson",r=>r.fulfill({json:{lesson:{mode:"lies",topic:"planets",requested_difficulty_tier:1,served_difficulty_tier:1,title:"Planets",sentences:[{id:"1",text:"Mercury is closest to the Sun."},{id:"2",text:"Mars is the largest planet."},{id:"3",text:"Earth has one moon."},{id:"4",text:"Jupiter is a gas giant."},{id:"5",text:"Saturn has rings."},{id:"6",text:"Neptune is far from the Sun."}],quiz_questions:[],safety_classification:{is_high_risk_topic:false,risk_reason:""}}}}));await page.route("**/api/adjudicate",r=>r.fulfill({json:{mode:"lies",plantedErrors:[{sentence_id:"2",error_tier:1,error_category:"blatant_fact",what_is_wrong:"",correct_version:"Jupiter is the largest planet."}],adjudication:{overall_score_percent:100,results:[],summary_explanation:"You caught it."}}}));await page.goto("/");await page.getByRole("button",{name:"I'm ready to investigate"}).click();await page.getByLabel("What do you want to learn?").fill("planets");await page.getByRole("button",{name:"Try a lesson"}).click();await page.getByRole("button",{name:/Mars is the largest planet/}).click();await page.getByRole("button",{name:"Submit my catches"}).click();await expect(page.getByText("100% caught")).toBeVisible();await expect(page.getByText("This lesson contained 1 deliberate errors")).toBeVisible();});
